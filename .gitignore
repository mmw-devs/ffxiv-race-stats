# OS
.DS_Store
Thumbs.db

# Editor
.vscode/
.idea/
*.swp
*.swo

# Claude Code — 本地开发指引，不提交远程
# ⚠ 受 .githooks/ 保护：误删后自动从 .git/safe/ 恢复
.claude/

# Backup files
*.bak

# Dependencies
node_modules/

# Static server logs (if any)
access.log
error.log

# PI Agent — 本地凭证（飞书 App Secret 等），不提交
# 仅忽略敏感配置；其余 .pi/ 内容（SYSTEM.md/extensions/prompts/rules/skills）为团队共享配置，仍纳入版本控制
.pi/settings.json

# Agent 会话日志（本地，不入仓）
.pi/sessions/

# Subagent artifacts（本地，不入仓）
.pi-subagents/

# agent-src/ 内嵌的 git 仓库元数据（不影响 ops 同步，sync workflow 通过 rsync 排除）
/agent-src/.git/

# Windows/WSL 元数据
*:Zone.Identifier

# 临时登录 QR（不入仓）
/auth-qr.png

# vendored npm 包目录（应在 .pi/npm/ 或 node_modules/）
/package/

dist/
.astro/
