---
description: >
  中文内容风格约束。在每次涉及 data.js 写入时自动生效。
  约束范围：中文规范、术语一致性、新闻格式、禁止项。
---
# 内容风格约束

此规则在每次涉及 `data.js` 写入时自动生效。

## 中文规范

- 所有面向用户的文本（name、title、eventName 等）使用简体中文
- 队伍名使用中文方括号占位格式 `[队伍名 N]`，运营提供真实名称后替换
- 赛事名称格式：`[副本名称] 世界首杀竞速`，如 `至天の座アルカディア LH 级 世界首杀竞速`

## 术语一致性

| 分类 | 规范 |
|------|------|
| **阶段命名** | 使用 `P1`、`P2`、`P3`、`P4`、`P5`、`P6`、`FINAL` 格式 |
| **区域** | `JP`（日本）、`NA`（北美）、`EU`（欧洲）、`OC`（大洋洲）、`CN`（中国）、`KR`（韩国） |
| **职业** | 使用官方三字母缩写：PLD/WAR/DRK/GNB/WHM/SCH/AST/SGE/MNK/DRG/NIN/SAM/RPR/BRD/MCH/DNC/BLM/SMN/RDM/VPR/PCT |
| **平台** | `bilibili`、`douyu`、`huya`、`twitch`、`youtube` |

## 新闻格式

- `title` 使用简洁陈述句，不超过 50 字
- `time` 格式 `YYYY-MM-DD HH:mm`
- `urgent: true` 仅用于首杀归属、重大规则变更等关键事件

## 禁止

- 禁止在数据中使用英文标点（使用中文全角标点）
- 禁止在 `name` 字段使用日文/英文（除非是国际队伍原名）
- 禁止编造非运营提供的真实数据——占位数据使用 `[...]` 格式
