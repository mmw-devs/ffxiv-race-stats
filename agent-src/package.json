{
  "name": "ffxiv-race-ops",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "FFXIV 高难首杀竞速 — 运营仓库（数据 + 运营 agent + 工作流 + 部署）",
  "scripts": {
    "validate": "tsx scripts/validate-data.ts",
    "validate-op-log": "tsx scripts/validate-op-log.ts",
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/node": "^26.2.0",
    "ajv": "^8.20.0",
    "dependency-cruiser": "^16.10.0",
    "tsx": "^4.23.12",
    "typescript": "^5.9.3",
    "vitest": "^4.1.11"
  }
}